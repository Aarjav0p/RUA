# ─────────────────────────────────────────────────────────────
#  RUA — Phase 1 · Python 3.14 compatible dependencies
#  Install: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────

# LLM (GPT-OSS-120B) + Whisper STT — Groq
groq

# Microphone input — PortAudio wrapper (bundles PortAudio on Windows)
sounddevice

# Audio array processing (energy-based voice activity detection)
numpy

# Text-to-speech — Microsoft Edge Neural voices (free, no API key)
edge-tts

# Audio playback — lightweight MP3/WAV/FLAC decoder + player (~1 MB)
miniaudio

# Environment variable loader (.env file support)
python-dotenv
